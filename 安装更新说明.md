# 📦 安装更新说明

## 🔄 更新内容

我们升级了PDF文本提取引擎，以解决乱码和提取失败的问题。

### 主要改进

1. ✅ 使用 `pdfplumber` 替代 `PyPDF2` 作为主要提取方法
2. ✅ 添加文本质量自动检测
3. ✅ 智能降级机制（多种方法自动切换）
4. ✅ 详细的诊断日志输出

## 🚀 快速安装

### 方法1: 安装单个新依赖（推荐）

```bash
pip install pdfplumber==0.11.0
```

### 方法2: 重新安装所有依赖

```bash
pip install -r requirements.txt
```

### 方法3: 使用 Conda

```bash
conda install -c conda-forge pdfplumber
```

## ✅ 验证安装

运行以下命令检查是否安装成功：

```python
python -c "import pdfplumber; print('✅ pdfplumber 安装成功')"
```

如果看到 `✅ pdfplumber 安装成功`，说明安装成功！

## 🎯 使用方法

### 1. 重启应用

```bash
python app_enhanced.py
```

### 2. 观察日志输出

上传PDF后，终端会显示详细的诊断信息：

```
[PDF提取] 开始提取文件: example.pdf
[PDF提取] 方法: pdfplumber (优先)
[PDF提取] 第 1/5 页 - 长度: 1234, 质量: 0.95
[PDF提取] 第 2/5 页 - 长度: 2341, 质量: 0.88
[PDF提取] 完成！共提取 5 页

[转换] 第 1 页文本质量: 0.95, 长度: 1234
[转换] 第 2 页文本质量: 0.88, 长度: 2341
```

### 3. 理解质量分数

- **0.7 - 1.0**: 优秀 ✅ - 文本提取质量很好
- **0.5 - 0.7**: 良好 🟡 - 可以使用，但可能有小问题
- **0.3 - 0.5**: 一般 ⚠️ - 质量较差，可能包含乱码
- **0.0 - 0.3**: 差 ❌ - 严重问题，可能是扫描版PDF

## 🔧 故障排除

### 问题1: 安装失败

```bash
# 错误: Could not find a version that satisfies the requirement

# 解决方案1: 升级pip
pip install --upgrade pip
pip install pdfplumber

# 解决方案2: 使用豆瓣源（国内用户）
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pdfplumber

# 解决方案3: 使用conda
conda install -c conda-forge pdfplumber
```

### 问题2: 导入错误

```python
# 错误: ModuleNotFoundError: No module named 'pdfplumber'

# 确认Python环境
which python  # Linux/Mac
where python  # Windows

# 确认pip安装位置
which pip  # Linux/Mac  
where pip  # Windows

# 如果环境不对，激活正确的虚拟环境
conda activate pdf2latex  # 如果使用conda
source venv/bin/activate  # 如果使用venv
```

### 问题3: 依赖冲突

```bash
# 如果出现依赖冲突，尝试创建新环境

# 使用conda创建新环境
conda create -n pdf2latex_new python=3.10
conda activate pdf2latex_new
pip install -r requirements.txt

# 或使用venv
python -m venv venv_new
source venv_new/bin/activate  # Linux/Mac
venv_new\Scripts\activate  # Windows
pip install -r requirements.txt
```

## 📋 完整依赖列表

更新后的 `requirements.txt`:

```txt
# Web框架
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5

# WebSocket支持
python-socketio==5.10.0
eventlet==0.33.3

# HTTP客户端
httpx==0.25.2

# PDF处理
PyPDF2==3.0.1
pdfplumber==0.11.0      # 新增 ✨
pdfminer.six==20221105  # 新增 ✨

# 环境变量管理
python-dotenv==1.0.0

# 工具库
werkzeug==3.0.1
```

## 🎉 预期效果

### 更新前

```
❌ 豆包模型识别出乱码
❌ GLM4.7消耗token但翻译失败
❌ 提取的文本质量差
```

### 更新后

```
✅ 使用更强大的提取引擎
✅ 自动检测并报告文本质量
✅ 智能降级到备用方法
✅ 明确提示扫描版PDF需要OCR
✅ 详细的诊断日志帮助排查问题
```

## 📞 需要帮助？

如果遇到问题：

1. 查看终端日志输出
2. 检查PDF文件类型（文本版 vs 扫描版）
3. 阅读 `PDF提取问题诊断与解决方案.md`
4. 确认所有依赖都正确安装

## 🔜 后续优化建议

如果仍有问题，可以考虑：

1. **添加OCR支持** - 使用 `pytesseract` 处理扫描版PDF
2. **使用Vision模型** - 直接让GPT-4o等视觉模型识别PDF图片
3. **PDF预处理** - 在转换前自动检测并优化PDF

现在先试试新的提取引擎，应该能解决大部分问题！🚀
