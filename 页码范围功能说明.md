# 页码范围输入功能说明

## 功能概述

现在页码输入框支持更灵活的输入格式，包括范围表示法和逗号分隔，使得选择页码更加方便快捷。

## 支持的输入格式

### 1. 单个页码
```
输入: 5
解析结果: 第5页
```

### 2. 逗号分隔的页码
```
输入: 1,3,5
解析结果: 第1,3,5页
```

### 3. 范围表示法（新增）
```
输入: 1-5
解析结果: 第1,2,3,4,5页
```

### 4. 混合格式（新增）
```
输入: 1-3,5,7-9
解析结果: 第1,2,3,5,7,8,9页
```

### 5. 复杂混合格式
```
输入: 1,3-5,8,10-12,15
解析结果: 第1,3,4,5,8,10,11,12,15页
```

## 功能特性

### ✅ 自动去重
如果输入了重复的页码，系统会自动去重：
```
输入: 1-3,2,3-5
解析结果: 1,2,3,4,5（自动去重）
```

### ✅ 自动排序
输出的页码会自动按升序排列：
```
输入: 5,1-3,9,7
解析结果: 1,2,3,5,7,9（自动排序）
```

### ✅ 错误提示
系统会检测并提示各种输入错误：

- **无效范围格式**
  ```
  输入: 1--3
  错误提示: 页码格式错误: 无效的范围格式: 1--3
  ```

- **起始页大于结束页**
  ```
  输入: 5-3
  错误提示: 页码格式错误: 起始页不能大于结束页: 5-3
  ```

- **无效的页码**
  ```
  输入: 1,abc,3
  错误提示: 页码格式错误: 无效的页码: abc
  ```

- **页码小于1**
  ```
  输入: 0-3
  错误提示: 页码格式错误: 页码必须大于0: 0-3
  ```

## 使用示例

### 场景1: 只需要前3页
**旧方式**: `1,2,3`
**新方式**: `1-3` ✨

### 场景2: 需要第1-5页和第10-15页
**旧方式**: `1,2,3,4,5,10,11,12,13,14,15`
**新方式**: `1-5,10-15` ✨

### 场景3: 需要前3页、第7页和最后2页（假设总共10页）
**旧方式**: `1,2,3,7,9,10`
**新方式**: `1-3,7,9-10` ✨

## 实现细节

### 核心解析函数
```javascript
function parsePageInput(input) {
    // 支持以下格式：
    // - 单页: "1" -> "1"
    // - 逗号分隔: "1,2,3" -> "1,2,3"
    // - 范围: "1-3" -> "1,2,3"
    // - 混合: "1-3,5,7-9" -> "1,2,3,5,7,8,9"
}
```

### 处理流程
1. 按逗号分割输入字符串
2. 对每个部分判断是范围还是单个页码
3. 如果是范围（包含"-"），展开为所有页码
4. 使用Set自动去重
5. 转换为数组并排序
6. 返回逗号分隔的字符串

## 界面更新

### 输入框提示文本
- **旧提示**: `例如: 1,2,3`
- **新提示**: `例如: 1-3,5,7-9` ✨

### 动态提示（检测到PDF页数后）
```
例如: 1-3,5,7-9 (共15页)
```

## 兼容性

✅ 完全向后兼容旧的逗号分隔格式
✅ 支持所有主流浏览器
✅ 错误处理完善，不影响正常使用

## 测试建议

1. **基础测试**
   - 输入 `1-3` 查看是否正确解析
   - 输入 `1,2,3` 查看旧格式是否仍然工作

2. **混合格式测试**
   - 输入 `1-3,5,7-9` 查看混合格式是否正确

3. **边界测试**
   - 输入 `5-3` 查看是否提示错误
   - 输入 `0-3` 查看是否提示错误
   - 输入 `abc` 查看是否提示错误

4. **去重和排序测试**
   - 输入 `3-1,5,2-4` 查看是否正确排序和去重

## 总结

这个优化大大提升了用户体验，特别是在需要选择连续多页时，可以用范围表示法快速输入，而不需要逐个输入页码。同时保持了向后兼容性，用户仍然可以使用原来的逗号分隔格式。
