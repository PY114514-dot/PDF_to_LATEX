# PDF2LaTeX 依赖包

# Web框架
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5

# WebSocket支持
python-socketio==5.10.0
eventlet==0.33.3

# HTTP客户端
httpx==0.25.2

# PDF处理
PyPDF2==3.0.1
pdfplumber==0.11.0  # 更强大的PDF文本提取
pdfminer.six==20231228  # pdfplumber 0.11.0 依赖的 PDF 解析库

# 图片处理和OCR
Pillow==10.1.0  # 图片读取、格式转换
pytesseract==0.3.10  # 本地OCR识别（Python封装）
opencv-python==4.10.0.84  # 图像预处理（已启用）
# 可选：扫描页/低质量页可用 PaddleOCR；PaddlePaddle 请按 CPU/GPU 平台安装
paddleocr==2.9.1
# Optional formula/mixed-layout provider (install only when enabled):
# pix2text>=1.1

# 注意：Tesseract OCR 引擎需要单独安装
# Windows: 下载安装 https://github.com/UB-Mannheim/tesseract/wiki
# 或使用 Chocolatey: choco install tesseract
# 或使用 Scoop: scoop install tesseract
# Linux: sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim
# macOS: brew install tesseract tesseract-lang

# 环境变量管理
python-dotenv==1.0.0

# 工具库
werkzeug==3.0.1

# 测试
pytest==9.1.1
pytest-asyncio==1.4.0
